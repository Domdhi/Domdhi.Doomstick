# Runtime — fetched from Mozilla-Ocho/llamafile releases
/runtime/
llamafile
llamafile.exe

# Model weights — fetched from huggingface.co/unsloth/... and ggml-org/...
/models/
*.gguf

# Legacy zipalign-bundled outputs and the standalone tool
/zipalign
*.llamafile
*.llamafile.exe

# Local test deployment when running build-usb.sh against the repo root
# (the build script writes ai-kit/ + start.* into its target dir; if you point
#  it at the repo itself, those land here and we don't want to commit them)
/ai-kit/
/start.bat
/start.command
/start.sh

# Test artifacts
/tmp/
*.log
*.pid

# Local debugging screenshots
/error.png
/screenshots-private/

# Editor / IDE
/.idea/
/.vscode/
.DS_Store
Thumbs.db
